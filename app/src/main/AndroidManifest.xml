<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".VulnerableBankApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:debuggable="true"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.VulnerableBankApp"
        tools:targetApi="31">
        <activity
            android:name=".SettingsActivity"
            android:exported="true"
            android:label="@string/settings_button"
            android:theme="@style/Theme.VulnerableBankApp" />
        <activity
            android:name=".HiddenFeatureActivity"
            android:exported="true"
            android:label="@string/hidden_feature_title"
            android:theme="@style/Theme.VulnerableBankApp" />
        <activity
            android:name=".SecurityInfoActivity"
            android:exported="true"
            android:label="@string/security_issues_button"
            android:theme="@style/Theme.VulnerableBankApp" />
        <activity
            android:name=".TransferActivity"
            android:exported="true"
            android:label="@string/make_transfer_button"
            android:theme="@style/Theme.VulnerableBankApp">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <data
                    android:host="transfer"
                    android:scheme="vuln" />
            </intent-filter>
        </activity>
        <activity
            android:name=".TransactionsActivity"
            android:exported="true"
            android:label="@string/view_transactions_button"
            android:theme="@style/Theme.VulnerableBankApp" />
        <activity
            android:name=".DashboardActivity"
            android:exported="true"
            android:label="@string/dashboard_heading"
            android:theme="@style/Theme.VulnerableBankApp" />
        <activity
            android:name=".LoginActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.VulnerableBankApp">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
