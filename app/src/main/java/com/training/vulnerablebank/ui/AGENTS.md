# AGENTS.md

Scope: `app/src/main/java/com/training/vulnerablebank/ui/`

## UI guidance
- Keep reusable UI pieces in `components/` and style tokens in `theme/`.
- Match existing Compose naming/style patterns in nearby files.
- Prefer minimal visual changes and avoid touching business logic from UI-only edits.
- If a UI change affects behavior, mention impacted screens in summaries.
